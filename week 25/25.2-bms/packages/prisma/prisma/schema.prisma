
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


// added user schema
model User {
  id            String  @id  @default(uuid())
  username      String  @unique
  password      String 
}